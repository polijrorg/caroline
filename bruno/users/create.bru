meta {
  name: create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/{{folder}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Aquinoo",
    "email": "{{$randomEmail}}",
    "password": "Teste123",
    "confirmPassword": "Teste123",
    "sobrenome": "aquino",
    "dataNasc": "2000-03-25T00:00:00.000Z"
  }
}

script:post-response {
  if (res.body && res.body.id) {
    bru.setVar("userId", res.body.id);
  } else {
    console.log("No ID found in response");
  }
}
